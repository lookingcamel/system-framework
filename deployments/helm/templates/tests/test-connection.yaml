apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "system-framework.fullname" . }}-test-connection"
  labels:
    {{- include "system-framework.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "system-framework.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
